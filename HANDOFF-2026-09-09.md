# nutrition-mind 인수인계 문서 (2026-09-09 작성)

> 후속 에이전트가 업무를 이어받기 위한 문서. 이 문서는 **세션에서 새로 학습한 사실과 현재 상태만** 담고,
> 스펙·운영 방식·검증 상세는 기존 아티팩트를 경로/URL로 참조한다 (중복 없음).

## 1. 프로젝트 현황 (한 줄)

로컬 Docker Compose 기반 건강기능식품 법령 RAG·MCP 데모가 **완성**되어 Epic #1이 종료되었고,
수용 기준 13/13이 충족되었다. 구성·실행법은 프로젝트 루트 `README.md`를, 스펙은 `seed.yaml`을 참조.

## 2. 표준 참조 문서 (중복 설명 대신 이 문서들을 읽을 것)

| 경로/URL | 내용 |
|---|---|
| `seed.yaml` | 프로젝트 스펙 (Goal/제약/수용기준 13건) |
| `project-background-context.md` | 배경 컨텍스트 + **API 실측 기록** (법령 DRF `efYd` 필수, 행정규칙 `target=admrul` 경로, 식품안전나라 09~19시 호출 차단 등) |
| `README.md` | 실행법, LLM 3-프로바이더 설정, 구현 현황 체크리스트 |
| `docs/obsidian-vault.md` | Obsidian Vault 운영 방식 + QMD 최소 연동 설계 |
| `backend/prisma/V8_MIGRATION_NOTE.md` | Prisma v8 마이그레이션 체크리스트 |
| `foodsafetykorea_schema/api_response_schema_doc.md` | 식품안전나라 API 필드 설명·응답코드 전표 |
| https://github.com/RaeunYu/nutrition-mind/issues | 이슈 #1~#8 전부 CLOSED — Task별 구현·검증 상세는 이슈 코멘트에 있음 |

## 3. 이번 세션에서 학습한 환경 제약 (기존 문서에 없음 — 반드시 숙지)

1. **Ouroboros** 워크플로우는 더이상 사용하지 않음. 목표에 도달하기 위한 끊임없는 루프로 인해 불필요한 동작을 다수 시도하는 것으로 확인함. (이전 대화에서 샌드박스 제한을 확인 했음에도, 간단한 권한 확인조차 시도하지 않고 제한을 회피하려는 시도 등)
2. **샌드박스 파일시스템 제약**:
   - 홈 디렉토리·`/opt/homebrew` 쓰기 불가 → `brew install`은 실패함 (사용자 터미널에서 실행 필요)
   - 프로젝트 내 `.git` 쓰기 불가 → **커밋은 사용자가 직접 수행** (지금까지의 운영 방식)
   - 프로젝트 내 `.agents`, `.codex`는 읽기 전용 → 프로젝트 스코프 스킬 설치 불가(전역 설치로 우회)
3. ~~**네트워크**: 사용자가 `~/.codex/config.toml`에 `[sandbox_workspace_write] network_access = true`를 설정한 뒤 세션을 재시작하여 열어둔 상태. 현재 네트워크 사용 가능.~~

   codex 에이전트도 현 시점부터 사용하지 않음
4. **npm 캐시 EPERM**: `~/.npm` 쓰기 차단 → `export npm_config_cache=/tmp/npm-cache` 필수.
5. **Docker**: Docker Desktop 구동 중(사용자 설치). 빌드/실행 시 아래 환경변수가 필요:
   ```bash
   export PATH="/opt/homebrew/bin:/Applications/Docker.app/Contents/Resources/bin:$PATH"
   export DOCKER_CONFIG=/tmp/dockercfg   # buildx activity 쓰기 EPERM 회피
   docker-compose up -d --build
   ```
6. **호스트 5432 포트 충돌**: 로컬 Homebrew Postgres가 5432를 점유 → 컨테이너 db는 **5433**으로 노출.
   호스트에서 DB 접속 시 포트 5433 사용 (`.env`의 `DATABASE_URL` 참조).
7. **Ollama**: `127.0.0.1:11434` 구동 중. `qwen3-embedding:0.6b`(dim=1024)와 `glm-5.3-flash:cloud`(챗 합성용) 설치됨.
   컨테이너에서는 `host.docker.internal:11434`로 접근.
8. **Python venv는 휘발성**: `/tmp/nm-venv` 사용 (psycopg/dotenv/requests 설치). 재부팅 후 사라졌다면:
   `python3 -m venv /tmp/nm-venv && /tmp/nm-venv/bin/pip install "psycopg[binary]" python-dotenv requests`
9. **gh CLI**: `/opt/homebrew/bin/gh`, 인증 완료(계정 RaeunYu). GitHub 이슈 읽기/쓰기 정상.

## 4. 현재 미결 상태

- **커밋 대기** (사용자가 리뷰 후 커밋 예정): `README.md` 수정(구현현황 체크 갱신 + "이 프로젝트를 만든 방식" 섹션 추가), `docs/obsidian-vault.md` 신규. 커밋은 **반드시 사용자가 수행**할 것.
- **`.env`**: `LLM_PROVIDER=ollama`, `LLM_MODEL=glm-5.3-flash:cloud`로 설정(동작 검증 완료).
  키 값은 이 문서에 없음 — 사용자가 직접 관리.
- **스킬 설치**: `npx skills@latest add mattpocock/skills` → **전역 `~/.agents/skills/`에 43개** 설치됨.
  프로젝트 `.agents`는 샌드박스 차단으로 생성 불가였음. Codex가 전역 경로를 인식하는지는 세션 재시작 후 미확인.

## 5. 후속 작업 후보 (우선순위 순)

1. 커밋 대기 항목 리뷰/커밋 (사용자)
2. **표시기준 별표 PDF 파싱** — PDF 형태가 다양해 카테고리화 불가 판단으로 별도 Epic으로 미룸.
   배경은 이슈 #3 종료 코멘트에 기록. 사용자가 별표·서식 확인 후 범위 확정 필요.
3. 챗봇 LLM 합성 고도화(현재: 근거 나열 + ollama 합성), 증분 갱신 스케줄러(19시 이후 cron) — 선택 사항
4. Obsidian Vault 실제 구성(사용자 환경에서 symlink 생성) — `docs/obsidian-vault.md` 체크리스트 참조

## 6. 추천 스킬 (스킬 도구로 요청할 것)

`~/.agents/skills/`에 Matt Pocock 스킬 컬렉션 43개가 설치되어 있음. 인수받은 에이전트는 상황에 맞게
아래 스킬을 skill 도구로 요청:

- **`code-review`** — 커밋 대기 중인 `README.md`·`docs/obsidian-vault.md` 변경분 리뷰 (사용자 검수 전 1차 검증)
- **`to-tickets`** — "별표 PDF 파싱"을 새 Epic/티켓으로 분해할 때 (PDF 형태 다양성 문제가 있어 조사 단계부터)
- **`retro`** — Epic #1 완료 회고. GitHub 이슈 #1~#8 히스토리를 입력으로 활용
- **`domain-modeling`** — 법령 도메인 용어집·ADR 추가 정비 시 (ADR 양식: `docs/obsidian-vault.md` 3절)
- **`find-skills`** — 위 외의 상황에서 적합한 스킬 탐색

## 7. 금지사항

- `.env`의 API 키 값 출력·복사 금지
- 사용자 승인 없이 커밋/푸시 시도 금지 (추후 git-manager 같은 별도 에이전트를 워크플로우에 도입할 가능성은 있음)
- Ouroboros 워크플로우 스킬 호출, MCP 도구 재시도 금지 (환경 제한 확정 — 시간 낭비)

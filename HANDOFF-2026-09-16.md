# 📋 Handoff — nutrition-mind Epic 3 완료 후 인수인계

> 작성: 2026-09-16 (Epic 3 — Tool Calling/Routing + 상거래 도메인 완료 시점)
> 후속 에이전트가 새 Epic을 시작하기 위한 문서. 중복 설명 대신 경로·이슈 번호로 참조한다.
> `.env`의 API 키·비밀 값은 절대 출력·복사 금지. 커밋·푸시 정책은 §8 참조.

---

## 1. 프로젝트 한 줄 요약

건강기능식품 법령·기능성 RAG·MCP 데모에서 출발해, **사내 담당자용 도구**(Epic 2)를 거쳐 **에이전트 하네스(Tool Calling 루프)와 Tool Routing/Selection**(Epic 3)을 갖춘 상태. 법령 RAG·역할 RBAC·개인정보 방법론 위에, 상담 담당자가 "이 고객 배송이 안 와요"라고 텍스트로 묻으면 카테고리를 라우팅해 해당 도구를 LLM에 주입하고, LLM이 도구를 선택·호출해 **실제 주문·결제·배송 데이터**로 답한다.

---

## 2. 현재 상태 (핵심 수치)

- **법령 RAG**: 조문 본문 148,470자, 임베딩 426/426, Recall@5 1.000 / MRR 0.833(13문항) — Epic 3에서 변경 없음
- **식약처 데이터**: 5개 API 93,273건, I0030 정규화 45,996건
- **상거래 시드(Epic 3)**: 고객 20명 · 주문 **39건** · 주문 항목 106 · 결제 39 · 배송 35 · 배송 이력 134 · 섭취 제품 106 · 관심 성분 27 — 제품 풀 25종(I0030)
- **MCP 도구**: **13종**(법령·기능성 4 · 배송 3 · 주문·결제 3 · 고객·성분 3), HTTP `/mcp`와 stdio 양쪽 노출
- **도구 라우팅**: 임베딩 **81.3%** / LLM **100%**(단일 의도 16문항), 복합 의도 top-2 병합 recall 87.5% / 100%
- **통합 테스트**: **16종 222케이스 전부 통과**(회귀 스크립트는 `backend/package.json`의 `test:*`)
- **Docker Compose 스택**: frontend 3000 · backend 3001 · mcp 8000 · db 5433(호스트)
- **마이그레이션**: 8건(`20260916083100_add_commerce_domain` 추가), `migrate status: up to date`

핵심 참조 문서:
- `README.md` — 실행법·데모 계정·역할별 화면·**Epic 3 데모 절차(도구 라우팅 A/B 토글)**
- `CONTEXT.md` — 도메인 용어집(주문·주문 항목·결제·배송·배송 이력·운송장 포함)
- `docs/adr/0001`·`0002`(개정 2회)·**`0003`(상거래 도메인과 도구 호출 하네스 — 비목표 위험 포함)**
- **`docs/tool-routing-eval.md`** — 라우팅 2기법 정량 비교·튜닝 함정
- `seed.yaml`(1차 스펙), `project-background-context.md` 2장(에이전트 하네스 개념), `docs/obsidian-vault.md`
- GitHub Epic 3: https://github.com/RaeunYu/nutrition-mind/issues/32 (T1~T7 = #33~#39)

---

## 3. Epic 3에서 추가된 코드

```
backend/src/
  mcp-tool-catalog.service.ts  — MCP tools/list 카탈로그 캐시(TTL 60s)·카테고리 그룹핑, tools/call 실행기(타임아웃·구조화 오류)
  tool-router.service.ts       — 임베딩 top-k / LLM 분류 라우터(교체 가능 인터페이스) + 임계값 미달 top-2 병합
  agent-harness.service.ts     — 하네스 루프: 도구 주입 → tool_calls 실행 → 재호출(A/B 모드)·trace·안전장치
backend/prisma/seed-commerce.ts — 상거래 시드(제품 풀 선정·주문/배송 시나리오·섭취 제품 파생)
backend/test/                   — mcp-tool-catalog.test.ts · tool-routing.test.ts(+goldenset.json) · agent-harness.test.ts
mcp-server/server.py            — 도구 13종(카테고리 `_meta.category` + 접근 로그)
frontend/app/chat/page.tsx      — 도구 추적 패널·A/B 토글·라우팅 방식 토글·지연 안내
```

**응답 계약** (`POST /chat/chat`): 요청 `{ question, customerId?, mode?: 'prefill'|'discover', router?: 'embedding'|'llm' }` / 응답 기존 필드 + `routing`·`trace`(하위호환 유지).

---

## 4. Epic 3 완료 작업

| 이슈 | 결과 | 검증 |
|---|---|---|
| #33 상거래 스키마·시드 | 5엔터티 + CHECK 4종, I0030 25종 풀, 39주문, 시나리오 5종+부분배송 | 멱등 재실행 동일, 고객당 주문 1~3·섭취 3~8 위반 0 |
| #34 MCP 도구 13종 | 배송·주문결제·고객성분 9종 추가, 카테고리 메타, 접근 로그 | HTTP·stdio 13종, actor 2경로, 갭 로직 백엔드 대조 일치 |
| #35 카탈로그·실행 계층 | tools/list 캐시, tools/call 구조화 실행, 재연결 | 신규 9케이스(타임아웃·오류·actor) |
| #36 라우터 2기법 | 임베딩 81.3% / LLM 100% | 골드셋 20문항, top-2 병합 87.5%/100% |
| #37 하네스 루프 | A/B 모드·재선언·최대 3회·강제 합성·trace | 스텁 LLM 6케이스 + 실 엔드투엔드 A/B 실제 배송지연 답변 |
| #38 챗봇 UI | 추적 패널·토글·지연 안내 | 서빙 번들 testid 7종, 라우트 200 |
| #39 검증·문서 | README·평가 문서·HANDOFF | **골드셋 사용자 검수만 남음** |

---

## 5. 남은 백로그 (우선순위 순)

1. **라우팅 골드셋 사용자 검수** — `backend/test/tool-routing-goldenset.json` 20문항을 사용자가 검토해 확정(Epic 3 #39의 마지막 항목)
2. **LangGraph 전환 판단(별도 에픽)** — 도구가 13종으로 늘어 배경 문서 기준(10개 이상)을 넘겼다. 순수 코드 하네스의 한계(상태 관리·조건 분기·재시도)를 실측한 뒤 전환 여부를 결정할 것
3. **Obsidian Vault 실제 도입** — 설계는 `docs/obsidian-vault.md`에 확정, 실행만 남음(전역 Vault 생성·symlink·ADR 이관)
4. **브라우저 시각 확인** — Epic 2·3 모두 번들/계약 수준까지만 검증. 스크린샷 기반 시각 비평은 사용자 확인 필요(도구 부재)
5. **MCP 요청 단위 인증** — ADR-0003의 명시적 비목표. 상거래·고객 도구가 인증 없이 외부에 노출된다(§6 참조)
6. **스트리밍(SSE)** — 하네스 루프는 논스트리밍. 지연이 체감되면 후속
7. 반품·교환·클레임 도메인(현재 비목표), 증분 갱신 스케줄러, LLM 합성 고도화 등 Epic 2 백로그 승계

---

## 6. ⚠️ 명시적 비목표 — MCP 외부 접근 인증 없음 (필독)

`mcp-server/server.py`의 도구는 **Claude Desktop(stdio)과 HTTP(`localhost:8000/mcp`) 양쪽에 인증 없이 노출**된다. `run_stdio.py`가 `server.py`의 `mcp` 인스턴스를 그대로 import하므로, 상거래·고객 도구를 추가하면 **외부 경로에서도 그대로 사용 가능**해진다.

- 실환경이라면 MCP 요청마다 별도 인증·권한 확인이 필요하다. **현 구현 목표에서 의도적으로 제외한 비목표**이며 구현 누락이 아니다(ADR-0003 「비목표로 남긴 위험」).
- 접근 로그는 MCP 도구 실행 지점에서 남지만(ADR-0002 개정), 이는 **사후 기록이지 접근 통제가 아니다**.
- README 「알려진 한계」 절에도 같은 내용이 사용자용으로 기록돼 있다.
- 후속 에픽에서 인증을 도입하면 **ADR-0003·ADR-0002·README 세 곳을 함께 갱신**할 것.

---

## 7. 이 세션에서 학습한 환경 특성 (기존 문서에 없음)

1. **Docker 소켓 경로**: 이 머신의 데몬 소켓은 `~/.docker/run/docker.sock`이다. 기존 핸드오프의 `export DOCKER_CONFIG=/tmp/dockercfg`를 쓰면 CLI가 `/var/run/docker.sock`을 찾아 실패한다. 대신:
   `export DOCKER_HOST=unix://$HOME/.docker/run/docker.sock` + `export PATH="/opt/homebrew/bin:/Applications/Docker.app/Contents/Resources/bin:$PATH"` 후 `docker-compose`(하이픈) 사용.
2. **Docker 빌드는 샌드박스 승인 필요**: 이미지 빌드가 `~/.docker/buildx/activity`에 쓰는데 workspace 샌드박스가 막는다(`operation not permitted`). `sandbox_permissions: danger-full-access`로 재시도해야 빌드된다. `docker-compose up`만으로는 재빌드되지 않으므로 코드 변경 후 `--build`를 붙일 것.
3. **Prisma v7에는 `migrate diff --shadow-database-url`이 없다.** 마이그레이션 폴더 기준 깨끗한 증분 SQL을 얻으려면:
   ① 스크래치 DB 생성 → ② `DATABASE_URL=<scratch> npx prisma migrate deploy` → ③ `DATABASE_URL=<scratch> npx prisma migrate dev --create-only --name X` → ④ 생성된 SQL에서 **`DROP INDEX idx_foodsafety_rows_*` 줄을 반드시 삭제**(Prisma가 표현 못 하는 수동 trgm GIN 인덱스를 지우려 한다) → ⑤ 실제 DB에 `migrate deploy`.
   `--from-config-datasource` 기반 diff는 무관한 드리프트(FK DROP·칼럼 삭제)까지 끌어오므로 쓰지 말 것.
4. **`tsconfig`가 `strict: false`(strictNullChecks off)** 라 `ok: true|false` 같은 **리터럴 판별 유니온 내로잉이 동작하지 않는다.** 선택 필드를 가진 단일 인터페이스를 쓰거나 `Extract<...>` 캐스트를 쓸 것(교훈: `ToolCallOutcome`).
5. **npm은 `export npm_config_cache=/tmp/npm-cache`로 동작**한다. `frontend/node_modules`가 비어 있을 수 있어 프론트 빌드 전 `npm install`이 필요하다. 프론트 Dockerfile은 `package-lock.json`을 복사하지 않는다(잠금 파일 커밋 안 함 — T6에서 생성된 부산물은 삭제함).
6. **`node:test`는 테스트 종료 시점까지 출력을 버퍼링**한다. 진행 상황을 보려면 개별 스크립트를 `timeout`으로 감싸고 종료 코드를 확인할 것.
7. **끊긴 MCP 세션을 기다리면 프로세스가 붙잡힌다** — `dropClients()`는 비블로킹으로 구현했다(끊긴 세션 await 금지). 타임아웃 테스트는 응답 없는 소켓 대신 **실서버 + 1ms 타임아웃**으로 검증한다.
8. **LLM 도구 호출 실측 특성**: `glm-5.3-flash:cloud`는 도구를 잘 고르지만 (a) 고객 이름만 주면 `customer_id`를 못 만들어 도구를 안 부르고 되묻는다 → **컨텍스트에 고객 UUID를 넣어 해결**했다. (b) discover 모드에서 `list_tools`를 여러 카테고리에 호출해 반복을 소진한다 → 발견 도구 **누적** + 한도 도달 시 **1회 강제 합성**으로 해결했다. 두 수정 모두 `agent-harness.service.ts`에 있다.
9. **임베딩 라우팅의 어휘 자기장**: 카테고리 설명에 여러 카테고리 공통 도메인어(예: "건강기능식품")를 넣으면 그 카테고리가 질의를 흡수한다(정확도 68.8%). 자세한 내용은 `docs/tool-routing-eval.md`.

---

## 8. 운영 규칙 (이슈·커밋 정책 — 사용자 확정)

- **커밋**: 에이전트가 제안 커밋 메시지(한국어, `feat:|fix:|docs:|polish: … (이슈 #N)`)로 직접 수행. **push는 금지**. 1인 프로젝트라 브랜치 충돌 여지 없음.
- **커밋 author**: repo `git config user.name`(raeun)·`user.email`(raeoon0511@naver.com) 그대로.
- **이슈 카드**: Epic 하위에 카드 생성(`ready-for-agent`) → 구현·검증 → 이슈 코멘트로 증거 기록 → close.
- **문서**: 한국어. 용어는 `CONTEXT.md` 준수(성분/원료 혼용 금지).
- **서브에이전트**: 병렬 가능 티켓은 백그라운드 위임. 장기 무응답 시 send_message로 재개 → 그래도 무진전이면 interrupt 후 재개하거나 오케스트레이터가 직접 인수.
  - 이번 Epic에서 T6(프론트 UI)을 서브에이전트에 위임해 성공했다(파일 범위를 `frontend/`로 한정하고 계약을 고정한 것이 주효). T4·T5는 인터페이스 결합이 커서 오케스트레이터가 직접 구현했다.
- **Ouroboros**: 사용하지 않음.
- **금지**: `.env` 값 출력·복사, push, 식품안전나라 openapi를 KST 09:00~19:00 호출, 대용량 CSV 전체 읽기, 원시 SQL로 암호화 우회, 검증 증거 없는 이슈 close.

---

## 9. 빠른 시작 (후속 에이전트 체크리스트)

1. `AGENTS.md` → `CONTEXT.md` → `README.md`(Epic 3 절) → `docs/adr/0003` → `docs/tool-routing-eval.md` 순으로 읽기
2. 스택 확인: `curl -s localhost:3001/health`, `localhost:8000/health`, `localhost:3000/chat`. 미구동 시 §7-1 환경변수로 `docker-compose up -d --build`
3. `cd backend && npx prisma migrate status` → `up to date` 확인
4. 기준선: `npm run test:harness`(6) · `npm run test:tool-routing`(4) · `npm run test:mcp-catalog`(9) 은 빠르고, 전체 회귀는 16종 222케이스
5. 새 티켓 착수: 이슈 카드 생성 → 구현 → 통합 테스트 → 검증 증거 기록 → close → 커밋

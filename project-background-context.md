# 프로젝트 배경 컨텍스트

> 이 문서는 스펙 인터뷰 에이전트(ouroboros 등)가 인터뷰를 시작하기 전 참고할 배경 컨텍스트입니다. 사전 조사와 검토를 거쳐 확정된 내용만 담고 있으며, "확정 사항"과 "미확정 사항(인터뷰에서 다룰 것)"을 구분해 표기했습니다. 히스토리(무엇이 어떻게 바뀌어 왔는지)는 기록하지 않고 현재 결론만 담습니다.
>
> **⭐ 대상 법령**: 이 프로젝트는 **① 건강기능식품에 관한 법률**, **② 식품 등의 표시ㆍ광고에 관한 법률**(각각의 시행령·시행규칙 포함), **③ 건강기능식품의 표시기준**(행정규칙/고시)을 핵심 대상으로 합니다. 아래 문서 곳곳의 조문 예시(예: "건강기능식품에관한법률 제3조")는 설계 방식을 설명하기 위한 개념 예시이며, 실제 대상 법령 확정 목록은 **1장**을 기준으로 삼아 주세요.

---

## 1. 대상 법령 및 데이터 소스

### 1.1 핵심 대상 법률 (2개)

이 프로젝트는 실제 기업 환경에서 운영되는 프로덕트가 아니라 **사이드 프로젝트**입니다. 다만 가상의 사용 시나리오는 **"건강기능식품" 관련 기업의 실무 담당자**(품질/인허가/마케팅 담당 등)가 법령·정책·기능성 정보를 조회하는 상황을 상정합니다.

| # | 법률명 | 담당 부처 | 이 프로젝트에서의 역할 |
|---|---|---|---|
| 1 | **건강기능식품에 관한 법률** (약칭: 건강기능식품법) | 식품의약품안전처 | 건강기능식품의 정의, 영업(제조업/판매업) 인허가, 기능성 원료 인정, 품목제조신고, 안전성/기능성 심사 등을 규율 — 이 프로젝트의 핵심 법률. |
| 2 | **식품 등의 표시ㆍ광고에 관한 법률** (약칭: 식품표시광고법) | 식품의약품안전처 | 건강기능식품을 포함한 식품 전반의 **표시 사항**(원재료명, 기능성 표시, 주의사항 표기 등)과 **광고 규제**(과대광고, 부당한 표시·광고 금지 등)를 규율. 인정받은 기능성을 실제로 어떻게 표기·광고할 수 있는지가 기업 실무 담당자에게 매우 중요한 확인 포인트이므로 핵심 법률로 선정. |

두 법률을 함께 다루는 이유: 건강기능식품법이 "무엇을 건강기능식품으로 인정하고 어떻게 제조·판매할 수 있는가"를 규율한다면, 식품표시광고법은 "그 제품을 라벨에 어떻게 표기하고 어떻게 광고할 수 있는가"를 규율합니다. 실제로 건강기능식품법 시행규칙 개정 이력에는 식품표시광고법 제정·개정에 따른 타법개정이 반복적으로 나타날 만큼 두 법률은 실무에서 밀접하게 맞물려 있습니다.

**두 법률의 하위 법령 체계 (총 6개 법령, `legal_provisions` 테이블의 `law_name` 확정값)**

| 법률 | 시행령 | 시행규칙 |
|---|---|---|
| 건강기능식품에 관한 법률 | 건강기능식품에 관한 법률 시행령 | 건강기능식품에 관한 법률 시행규칙 |
| 식품 등의 표시ㆍ광고에 관한 법률 | 식품 등의 표시ㆍ광고에 관한 법률 시행령 | 식품 등의 표시ㆍ광고에 관한 법률 시행규칙 |

**「건강기능식품의 표시기준」(행정규칙/고시) — 1차 커버리지 추가 대상**

위 6개 법령(법률 2개 + 시행령 2개 + 시행규칙 2개)과 별도로, **「건강기능식품의 표시기준」**을 1차 커버리지에 포함합니다. 이 문서는 식약처 고시(행정규칙)로, 법률·시행령·시행규칙과는 법적 위계와 제개정 절차가 다릅니다(행정규칙은 국회 심의를 거치지 않고 소관 부처가 직접 고시하는 방식이라 절차가 더 가볍고 개정 빈도가 상대적으로 높을 수 있음). 그럼에도 이 문서를 1차 대상으로 격상한 이유는, 제품의 함량·단위 등 **필수 표시사항의 실제 표기 방법**을 규정하고 있어 실무 중요도가 앞의 2개 법률에 준한다고 판단했기 때문입니다.

- 발견 경로: 원래는 `식품 등의 표시ㆍ광고에 관한 법률 시행규칙 제6조제5항`이 위임하는 참조 대상으로, 1.2절의 하이브리드 방식(별도 노드 + 참조 엣지)으로만 다뤄질 예정이었습니다. 그러나 실무 중요도를 고려해 **참조를 따라가야 발견되는 2차 문서가 아니라 처음부터 전체 수집 대상인 1차 문서**로 취급하도록 변경합니다. 이 판단 배경을 인터뷰 에이전트에게 사전 고지합니다.
- `legal_provisions` 테이블 관점: `law_type` 값에 `law`/`enforcement_decree`/`enforcement_rule`과 구분되는 `notification`(고시/행정규칙) 같은 값을 추가하는 것을 검토(세부 스키마는 인터뷰에서 확정).
- 국가법령정보센터가 이런 고시·행정규칙까지 법률/시행령과 동일한 방식으로 조회 가능한지(전용 API 엔드포인트 유무, 조문 구조 동일 여부)는 **인터뷰에서 재확인이 필요**합니다.

### 1.2 참조 대상 법령 (2차 대상) — 조문 참조 처리 방식(1.4절)을 그대로 적용

위 법령들의 조문을 실제로 읽다 보면 다른 법률을 참조하는 조문이 반드시 등장합니다. 이런 참조 대상 법령은 **본문에 inline으로 합치지 않고**, 1.4절에서 정의한 **"별도 노드 + 명시적 참조 엣지"** 하이브리드 방식으로 처리합니다.

- **대통령령/총리령/부령 일반** — "~은 대통령령으로 정한다", "~은 총리령으로 정한다" 같은 위임 조항. 대부분 위 표의 시행령·시행규칙으로 연결되나, 간혹 다른 부처 소관 법령을 가리킬 수도 있음 → `reference_type: delegates_to`. **총리령**은 대통령령만큼 널리 알려져 있지는 않지만 식약처 고시 체계에서 대통령령 못지않게 중요하게 다뤄지므로, 위임 조항을 처리할 때 "대통령령"만 인지하고 "총리령"을 놓치지 않도록 인터뷰·구현 단계에서 함께 확인이 필요합니다.
- **식품위생법** — 건강기능식품법이 식품 전반의 기본법인 식품위생법의 특별법적 성격을 가지므로, 정의 규정이나 영업 관련 조항에서 인용되는 경우가 있음 → `reference_type: cites`
- **전자상거래 등에서의 소비자보호에 관한 법률(전자상거래법)** — 건강기능식품의 온라인 판매·구매대행, 통신판매 관련 조항에서 인용될 수 있음 → `reference_type: cites`
- **표시·광고의 공정화에 관한 법률(공정거래위원회 소관)** — 식품표시광고법과 이름이 유사하나 소관 부처와 규율 대상이 다른 **별개의 법률**이므로 혼동 주의. 부당광고 관련 조항에서 상호 참조될 가능성 있음.

**수집 방침**
1. 1.1절의 핵심 법령(2개 법률 + 시행령 2개 + 시행규칙 2개 + 「건강기능식품의 표시기준」)은 **전체 조문을 수집**하여 `legal_provisions`에 정식 노드로 등록.
2. 참조 대상 법령(식품위생법, 전자상거래법 등)은 전체 조문을 수집할 필요는 없으며, 핵심 법령이 실제로 참조하는 특정 조문만 필요 시점에 개별 수집하여 `law_type: referenced_external` 같은 별도 구분값으로 등록(1.4절 스키마의 `law_type` enum 확장). 참조되지 않는 나머지 조문까지 전부 수집하는 것은 불필요한 낭비이므로 지양.
3. 참조 확장(2-hop) 조회 시 참조 대상이 핵심 법령 내부인지 외부 법률인지에 따라 응답에서 "관련 법령(핵심)"과 "참고 법령(외부)"을 구분해 제시하는 것을 인터뷰에서 논의할 가치가 있음.

### 1.3 데이터 소스 — 확정 법령 (국가법령정보센터)

- 제공처: **국가법령정보센터** (`https://open.law.go.kr`)
- 상태: **API Key 발급 완료**
- 수집 대상: 1.1절의 핵심 법령(법률 2개 + 시행령 2개 + 시행규칙 2개 + 「건강기능식품의 표시기준」) 전체 조문 + 1.2절의 참조 시 확장 수집되는 2차 대상 법령
- **시행 예정 조문도 이 API 하나로 커버 가능** — 국가법령정보센터는 현재 시행 중인 조문뿐 아니라, 이미 공포되었지만 시행일이 도래하지 않은 개정 조문도 해당 법령 페이지에서 함께 확인할 수 있습니다. 조문 하단에는 부칙 개정 이력이 남아 있고, 시행일이 남은 개별 조문에는 `[시행일: YYYY. M. D.] 제OO조` 형식으로 시행 예정임이 명시됩니다.
  - 실제 확인 사례: 「조달사업에 관한 법률 시행령」(대통령령 제36632호, 2026.9.1. 일부개정)의 경우, 법령 페이지 본문 상단은 `[시행 2026. 9. 3.]`이지만 제44조(과태료의 부과기준)만 별도로 `[시행일: 2026. 9. 11.]`이 명시되어 있고, 개정 전/후 조문이 함께 표기됨. 부칙에도 "이 영은 2026년 9월 11일부터 시행한다. 다만, 제36조의2의 개정규정은 2026년 9월 3일부터 시행한다"처럼 조문별 시행일 차이가 기록됨.
  - 이 방식을 활용하면 별도의 관보 API 없이도 **"곧 시행될 개정 조문"을 국가법령정보센터 API 하나로 조회**할 수 있습니다. `legal_provisions` 테이블에 조문의 시행일자(`effective_date`)와 함께, 필요 시 "공포일" 필드를 추가해 아직 시행 전인 개정 조문을 구분·안내하는 것을 검토할 수 있습니다(세부 스키마는 인터뷰에서 확정).

> 참고로 조사 과정에서 전자관보(`https://gwanbo.go.kr`)와 국민참여입법센터(법제처 운영, 국회 제출 이전 단계의 사전 절차 — 법제처 심사 및 대국민 의견 청취 등을 거친 뒤 국회에 제출되는 구조) API도 검토했으나, 국가법령정보센터 하나로 필요한 정보(확정 조문 + 시행 예정 조문)를 모두 확인할 수 있어 **채택하지 않았습니다.** 국회 의안(법안) 진행 상태 추적 및 입법예고/행정예고 단계 추적은 폐기·중복발의·절차적 노이즈가 크다고 판단해 현재 스코프에서 제외합니다. 다만 향후 버전에서 필요성이 재확인되면 별도 기능으로 추가될 수 있으므로, 이 판단은 기록만 해둡니다.

### 1.4 법령/시행령 조문의 참조 관계 처리 — RAG 설계

**문제**: 국가법령정보센터 조문에는 "대통령령으로 정하는 바에 따라", "총리령으로 정하는 바에 따라" 같은 문구로 시행령·시행규칙·다른 조항을 참조하는 경우가 있음. 임베딩/벡터화 저장 시 이 참조를 어떻게 처리할지가 쟁점.

**결론(하이브리드 방식)**: 참조를 따라가 텍스트를 통째로 inline시키지 않고, **별도 노드로 두고 참조는 명시적 관계(엣지)로만 연결**하는 것을 기본으로 하되, 검색 시 필요한 경우만 확장 조회.

**inline시키면 안 되는 이유**
- 참조를 계속 따라가면 시행령 → 시행규칙 → 또 다른 조항 참조로 재귀적으로 뻗어나가 청크가 거대해지고 검색 관련성이 떨어짐(법률 본문이 시행령 조문에 묻힘).
- 법률과 시행령/시행규칙은 개정 주기가 다름. 한 청크로 합치면 시행령만 바뀌어도 법률 청크 전체를 재임베딩해야 하는 비효율 발생.

**스키마 설계 (PostgreSQL + pgvector, self-referencing junction table)**
```sql
-- 조문 테이블
CREATE TABLE legal_provisions (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  law_name        TEXT NOT NULL,  -- '건강기능식품에 관한 법률' 등 1.1절의 확정값 + 참조 시 추가되는 외부 법령명
  law_type        TEXT NOT NULL,  -- 'law' | 'enforcement_decree' | 'enforcement_rule' | 'notification' | 'referenced_external'
  article_no      TEXT NOT NULL,  -- '제3조'
  content         TEXT NOT NULL,
  embedding       vector(1536),
  effective_date  DATE,           -- 시행일자
  promulgated_date DATE,          -- 공포일자 (시행 전 조문 구분용)
  created_at      TIMESTAMPTZ DEFAULT now(),
  updated_at      TIMESTAMPTZ DEFAULT now()
);

-- 참조 관계 (그래프의 엣지 역할)
CREATE TABLE provision_references (
  source_id       UUID REFERENCES legal_provisions(id) ON DELETE CASCADE,
  target_id       UUID REFERENCES legal_provisions(id) ON DELETE CASCADE,
  reference_type  TEXT,  -- 'delegates_to' (위임), 'cites' (단순 인용), 'amends' (개정)
  raw_phrase      TEXT,  -- 원문에서 추출된 문구, 예: "대통령령으로 정하는 바에 따라"
  PRIMARY KEY (source_id, target_id, reference_type)
);

CREATE INDEX ON legal_provisions USING hnsw (embedding vector_cosine_ops);
CREATE INDEX ON provision_references (source_id);
CREATE INDEX ON provision_references (target_id);
```

**RAG 쿼리 예시 (2-hop 확장)**
```sql
WITH matched AS (
  SELECT id, law_name, article_no, content, law_type,
         embedding <=> $1 AS distance
  FROM legal_provisions
  ORDER BY embedding <=> $1
  LIMIT 5
),
expanded AS (
  SELECT lp.id, lp.law_name, lp.article_no, lp.content, lp.law_type,
         pr.reference_type, matched.id AS expanded_from
  FROM provision_references pr
  JOIN legal_provisions lp ON lp.id = pr.target_id
  JOIN matched ON matched.id = pr.source_id
)
SELECT * FROM matched
UNION ALL
SELECT id, law_name, article_no, content, law_type, NULL, NULL FROM expanded;
```
- **Retrieval Node**: 위 쿼리로 원본 매칭 조문 + 참조 확장 조문 획득
- **Synthesis Node**: 원 조문과 참조 확장 조문을 구분해 프롬프트에 삽입(예: "질의와 직접 관련된 조문" / "해당 조문이 위임하는 시행령")

**Neo4j 등 별도 그래프 DB는 현재 불필요** — 1~2hop 확장 정도는 PostgreSQL CTE로 충분하며 인프라가 단순해 운영 부담이 적음. 나중에 "최단 경로", "가장 많이 참조되는 조문 랭킹" 같은 진짜 그래프 알고리즘이 필요해지면 재검토할 수 있으나, 이 판단은 기록만 해두고 현재는 PostgreSQL + pgvector로 시작합니다.

### 1.5 데이터 소스 — 건강기능식품 기능성 정보 (식약처)

- 제공처: **식품의약품안전처 식품안전나라 공공데이터 활용 플랫폼** (`https://www.foodsafetykorea.go.kr/api/datasetList.do?menu_grp=MENU_GRP31&menu_no=17879`)
- 상태: **API Key 발급 및 사용 승인 완료**
- 활용 대상 데이터셋 (4종, 서비스명 포함)

| 서비스명 | 데이터셋 이름 | 목적/역할 |
|---|---|---|
| I-0050 | 건강기능식품 개별인정형 정보 | 개별 업체가 심사를 거쳐 인정받은 원료의 상세 정보(원료인정번호, 1일 섭취량 상한/하한, 원재료명, 섭취 시 주의사항, 주된 기능성 등) |
| I-0040 | 건강기능식품 기능성 원료인정현황 | 고시형/개별인정형을 포함한 전체 기능성 원료 인정 현황 |
| I0030 | 건강기능식품 품목제조 신고사항 현황 | 건강기능식품 제조사들의 실제 제조 성분(원료) 동향을 통계로 제공하는 기능 구현 목적 |
| C003 | 건강기능식품 품목제조신고(원재료) | 위와 동일 목적, 품목별 원재료 상세 데이터 |

- **API 스펙과 요청/응답 스키마는 API 승인 완료 후 확보하여 스펙 인터뷰 에이전트에게 별도로 제공할 예정**입니다. 인터뷰 시점에 이 정보가 아직 없다면, 사용자에게 확보 여부를 먼저 확인해 주세요.

**개별인정형 vs 고시형 (도메인 용어집 후보)**: 건강기능식품 원료는 크게 **고시형**(식약처가 「건강기능식품 공전」에 기준·규격을 미리 고시해 누구나 사용 가능한 원료)과 **개별인정형**(개별 업체가 임상 등 과학적 근거로 신청해 심사를 거쳐 인정받은, 해당 업체만 사용 가능한 원료)으로 나뉩니다. "새로 인정된 기능성 원료 정보"는 주로 개별인정형 원료의 신규 인정 건을 추적하는 것에 해당할 가능성이 높습니다 — 고시형은 등재 빈도가 낮은 반면 개별인정형은 계속 새로운 원료가 인정되는 영역이기 때문입니다. 다만 고시형/개별인정형 둘 다 다룰지 개별인정형 위주로 할지는 인터뷰에서 확정합니다.

**RAG vs 단순 MCP 조회 판단 기준 (제안, 최종 결정은 인터뷰에서)**

| 질의 유형 예시 | 적합한 방식 | 이유 |
|---|---|---|
| "OO 원료가 인정된 기능성이 뭐야?" / "이 원료 1일 섭취량 상한선은?" | 구조화 조회(단순 MCP tool) | 원료인정번호나 원료명으로 정확히 매칭되는 필드 조회 |
| "피로 개선에 도움되는 기능성 원료 뭐가 있어?" | RAG(임베딩 기반 검색) | "주된 기능성" 텍스트가 자유 서술형이라 자연어 질의와 정확히 일치하지 않을 가능성이 높음 |
| "최근에 새로 인정된 원료가 있어?" | 구조화 조회 + 시계열 필터 | 등록일자 기준 정렬·필터링 문제 |

→ "기능성" 텍스트 필드만 임베딩해 의미 검색에 활용하고, 나머지(원료명, 섭취량, 날짜 등)는 관계형 컬럼으로 직접 조회하는 하이브리드가 1.4절과 일관된 패턴으로 자연스러워 보입니다. RAG 채택 여부 자체는 인터뷰에서 최종 확정합니다.

#### 1.5.1 데이터 접근 제약사항

식품안전나라 OpenAPI는 아래와 같은 구조적 제약을 가지고 있으며, I-0030·C003 두 데이터셋은 I-0050·I-0040에 비해 데이터 양이 훨씬 방대해(수만 건 단위) 이 제약이 실질적인 설계 문제가 됩니다.

- **요청 URL 포맷**: `http://openapi.foodsafetykorea.go.kr/api/인증키/서비스명/요청파일타입/요청시작위치/요청종료위치`
- **정렬 기준 제어 불가**: 요청 파라미터에 정렬 기준을 지정하는 항목이 없습니다. 응답에는 `품목제조번호` 같은 고유 ID와 `total_count`(전체 건수)가 포함되지만, 명확한 정렬 기준(신청일자, 허가일자 등)은 확인되지 않습니다. 정렬 기준을 알 수 없으므로 `ORDER BY ID DESC LIMIT 5` 같은 방식의 페이지네이션은 사용할 수 없습니다.
- **`요청시작위치/요청종료위치`의 실제 동작**: 이 값은 응답 데이터나 CSV에 별도로 노출되지 않는 것으로 보아, 식품안전나라 내부에서 순차적으로 쌓인 인덱스를 그대로 청킹 기준으로 쓰는 것으로 추정됩니다(별도로 인덱스를 기록해두지 않는 한 어떤 row가 몇 번 인덱스인지 사후에 확인하기 어려움). 확인된 동작 규칙은 다음과 같습니다.
  - 0번 인덱스는 존재하지 않으며 1번부터 시작.
  - `1/1` 요청 시 1번 row 1개 반환.
  - `251/251` 요청 시 251번 row 1개 반환.
  - `251/253` 요청 시 251, 252, 253번 row 3개 반환.
  - 마지막 인덱스가 447번일 때 `444/500` 요청 시 444~447번 row 4개만 반환(종료 인덱스가 실제 마지막 인덱스보다 커도 존재하는 데이터까지만 반환됨).
- **호출 횟수 제한**: OpenAPI 서비스는 **1시간 내 100회를 초과 호출하면 1시간 동안 호출이 제한**됩니다.
- **서비스 가능 시간대 관련 오류코드 잔존**: 과거 별도 공지 시까지 서비스 제공 시간을 `09:00~19:00`으로 제한한다는 공지가 있었습니다. 현재는 해당 시간 외에도 호출이 가능한 것으로 확인되지만, 응답 스키마의 오류코드 안내에는 이 시간 제한과 관련된 오류코드가 여전히 남아 있습니다. 이 오류코드에 대한 방어 로직(재시도, 에러 핸들링)은 구현 단계에서 준비해 두는 것이 안전합니다.

#### 1.5.2 데이터 적재 전략 (제안 — CSV 사전 적재 + 증분 감지)

위 제약(정렬 불가, 대량 데이터, 호출 횟수 제한)을 고려해 4개 데이터셋(I-0050, I-0040, I0030, C003) 공통으로 아래 전략을 적용하는 것을 제안합니다. 식품안전나라는 OpenAPI로 제공하는 모든 데이터셋을 **CSV 형태로도 함께 제공**합니다.

**전제 및 공통 흐름**
1. 프로젝트 진행 초기, 사용자가 4개 데이터셋의 CSV 파일을 프로젝트 내부 디렉토리에 미리 저장해 둡니다(파일 크기와 민감도를 고려해 `.gitignore` 처리 필요 — 반드시 확인).
2. 최초 1회, 저장된 CSV를 파싱해 DB에 적재하고, 적재 시점의 `total_count`를 별도로 함께 저장해 둡니다.
3. 이후에는 API 호출을 최소화합니다 — 좁은 범위(예: `1/1`)로 호출해도 응답에는 **전체 row 수를 나타내는 `total_count`가 포함**되므로, 이 값만으로 변경 여부를 감지할 수 있습니다.
4. DB에 저장된 마지막 `total_count`와 현재 API 응답의 `total_count`가 다르면, 늘어난 범위만큼만 인덱스를 지정해 호출하여 신규 데이터를 갱신합니다.
5. 예외적으로 I-0040(기능성 원료인정현황)은 `PRMS_DT`(인정일자) 파라미터로 특정 날짜 이후 데이터만 조회하는 것이 가능합니다. 다만 인정 건수 자체가 연간 100건 미만으로 적고, 4개 API에 동일한 갱신 구조를 적용해 구현 일관성을 확보하는 것이 우선이라고 판단해, I-0040도 공통 방식(전체 재조회 기반 증분 감지)을 그대로 적용합니다.
6. 4개 데이터셋의 공식적으로 명시된 고유 ID 필드는 없지만, `HF_FNCLTY_MTRAL_RCOGN_NO` 필드를 고유 ID로 활용할 수 있는 것으로 보입니다(문자열 타입).

**참고용 스냅샷 수치 (2026-09-05 기준, `total_count`)** — 인터뷰 시점에는 값이 달라져 있을 수 있으므로 참고용으로만 사용하세요.

| 서비스명 | 데이터셋 | 09/05 기준 total_count |
|---|---|---|
| I-0050 | 건강기능식품 개별인정형 정보 | 447건 |
| I-0040 | 건강기능식품 기능성 원료인정현황 | 773건 |
| I0030 | 건강기능식품 품목제조 신고사항 현황 | 45,996건 |
| C003 | 건강기능식품 품목제조신고(원재료) | 45,996건 |

**트레이드오프 (인터뷰에서 함께 논의할 사항)**
- **장점**: 시간당 100회 호출 제한을 회피하며 API 호출을 최소화할 수 있고, 초기 대량 데이터(I0030/C003의 4만 건대)를 CSV로 안정적으로 적재할 수 있으며, 4개 API에 동일한 패턴을 적용해 구현 일관성을 확보할 수 있습니다.
- **한계**: `total_count`만으로는 "몇 건이 늘었는지"는 감지할 수 있어도, **기존 row 중 일부의 내용이 수정되었는지는 감지하지 못합니다**(건수가 그대로면 변경이 없다고 오판할 수 있음). 또한 I-0040처럼 날짜 필터가 가능한 API까지 굳이 전체 재조회 기반 방식으로 통일하는 것은 구현 일관성은 얻지만 최적의 호출 효율은 아닙니다 — 이는 의도적으로 감수하는 트레이드오프로 기록해 둡니다.
- **고유 ID 필드 관련 주의**: `HF_FNCLTY_MTRAL_RCOGN_NO`(명칭상 "기능성 원료 인정번호" 계열)는 I-0050/I-0040(원료 인정 계열 데이터셋)에는 자연스러워 보이지만, I0030/C003(품목제조 신고 계열 데이터셋)에서도 동일한 필드명이 그대로 고유 ID로 쓰이는지는 확인되지 않았습니다. 4개 데이터셋의 고유 ID 필드가 실제로 동일한지, 데이터셋별로 다른 필드를 식별자로 써야 하는지는 **API 응답 스키마 확보 후 재검증이 필요**합니다.

이 전략이 비효율적이거나 트레이드오프가 과도하다고 판단되면, 인터뷰 에이전트가 대안(예: 스케줄 기반 전체 재적재, 데이터셋별 개별 전략 등)을 제시하는 것도 검토할 수 있습니다.

---

## 2. 학습 목적 기술 스택

### 백엔드 ORM (2026-09-08 확정)

- 백엔드 DB 접근은 **Prisma v7**로 통일한다. v8은 RC 상태라 불확실성이 있어 고정하지 않으며, 정식 출시 후 `backend/prisma/V8_MIGRATION_NOTE.md`의 체크리스트에 따라 마이그레이션한다.
- pgvector 필드(`vector(1024)`)는 Prisma `Unsupported` 타입이므로 임베딩 기록/검색은 원시 SQL로 수행한다.


프로젝트를 진행하며 아래 기술들을 실전에서 익히는 것이 목적 중 하나입니다. 인터뷰 시 이 학습 목적을 고려해 주세요(예: 지나치게 쉬운 지름길보다 학습 가치가 있는 구조를 함께 제안하는 것이 도움이 됨).

- **Obsidian** (Vault 설계 — 3장 참고)
- **RAG (Retrieval-Augmented Generation)**
- **LangGraph** — RAG/에이전트 오케스트레이션 프레임워크
- **Tool Calling / Tool Routing & Selection**
- **FastMCP** — RAG 기능을 MCP 서버로 노출
- **pgvector** — PostgreSQL 기반 벡터 검색 + 관계형 데이터 결합

### 2.1 에이전트 하네스 / Tool Calling / MCP 기본 개념

**핵심 개념 — "에이전트 하네스"**: LLM 서빙만으로는 부족하며, 반드시 **에이전트 하네스**(LLM ↔ 도구 실행을 오가며 루프를 도는 오케스트레이션 로직)가 필요합니다. 하네스는 개념적 이름이고, 실체는 백엔드 코드 자체입니다(별도의 독립된 컴포넌트가 아님).

루프 구조: ① 사용자 질문 + 도구 스키마를 LLM에 전달 → ② LLM이 텍스트 답변 또는 `tool_use`(도구 호출 의도, JSON) 반환 → ③ 백엔드가 이를 파싱해 실제 함수/MCP 서버 호출 실행 → ④ 결과를 다시 LLM 컨텍스트에 넣어 재호출 → ⑤ 최종 자연어 답변을 사용자에게 서빙.

**중요**: LLM은 백엔드 함수를 직접 호출하지 못합니다. "이 함수를 이 인자로 부르고 싶다"는 의사표시(JSON 텍스트)만 생성하며, 실제 실행은 100% 백엔드(하네스)의 몫입니다.

**3계층 구조**

| 계층 | 역할 | 예시 |
|---|---|---|
| LLM 서빙 | 순수 추론, 도구 호출 스키마 이해 | Ollama, vLLM, Claude API 등 |
| 에이전트 하네스 | 루프 관리, 도구 실제 실행, 상태 유지, 권한/로깅 | 자체 백엔드, LangGraph, Claude Agent SDK |
| MCP 서버(도구) | 실제 비즈니스 로직 수행 | 법령 조회 API, RAG 검색 등 |

**도구 스키마 주입 vs 지식 주입**
- **층위 1 — 도구 스키마**: 매 요청마다 사용 가능한 함수의 이름/설명/파라미터를 JSON으로 첨부하는 단순한 문제. MCP 서버는 `tools/list` 표준 엔드포인트로 자신의 도구 목록을 노출하므로 하네스가 자동으로 받아올 수 있음.
- **층위 2 — 조직 지식/비정형 문서**: "정책, 매뉴얼" 같은 걸 LLM이 참고하게 하는 문제. 여기서 RAG, 지식그래프가 등장. 실무에서는 이 지식 검색 자체도 `search_legal_provisions(query)` 같은 **하나의 MCP tool**로 감싸서 층위 1의 스키마 목록에 등록.

### 2.2 Tool Routing / Selection

도구가 많아지면(대략 20개 이상) 컨텍스트 낭비와 정확도 저하가 발생하므로 관련성 높은 일부만 골라 LLM에 제공하는 로직이 필요합니다.

| 방식 | 설명 |
|---|---|
| 카테고리/도메인 기반 사전 필터링 | 질문 의도를 먼저 분류 → 해당 카테고리 도구만 로드. 가장 단순, 초심자 추천. |
| 임베딩 기반 유사도 검색 | RAG와 원리가 동일. 도구 설명을 임베딩해두고 질문과의 코사인 유사도로 top-k 선택. |
| 계층적 라우팅(서브 에이전트 분할) | 도구가 수백 개 단위인 대규모 환경에서 사용. |

이 프로젝트 관점에서는 "확정 법령 조문 검색 도구"와 "기능성 정보 조회 도구"를 별개의 MCP tool로 분리 등록하고, 질문 의도에 따라 라우팅하는 것을 권장합니다.

### 2.3 Tool Routing vs LangGraph — 서로 다른 층위

- **Tool routing/selection**: 하네스 안의 "필터링 로직" 한 조각.
- **LangGraph**: 하네스 전체를 짜는 프레임워크(도구 실행 루프, 상태 관리, 에러 처리, 조건 분기 전체를 담는 그릇). 둘은 경쟁 관계가 아니라 포함 관계.

**LangGraph 도입 판단 기준**

| 상황 | 선택 |
|---|---|
| 도구 1~3개, 단순 대화형 | 순수 코드 직접 구현 |
| 도구 10개 이상, 복잡한 분기/상태 필요 | LangGraph 같은 프레임워크 |
| 팀에 LLM 오케스트레이션 경험 적음 | 프레임워크의 검증된 패턴 활용 |
| 매우 특수한 커스텀 요구사항, 성능 최적화 중요 | 순수 코드 (또는 프레임워크 저수준 API 일부 활용) |

**LangGraph 장점**: 내장 State 관리, 선언적 조건 분기, 재시도/체크포인트, 관측성(LangSmith), 병렬 도구 호출, 생태계.
**트레이드오프**: 학습 비용(그래프/State/노드·엣지 멘탈모델), 블랙박스화로 인한 디버깅 난이도, 단순 요구사항엔 오버엔지니어링 위험.

### 2.4 Graphify 사용 범위 (코드베이스 온보딩 전용)

**Graphify는 코드베이스를 지식 그래프로 변환해서 AI 코딩 에이전트가 grep 대신 그래프를 질의하도록 만드는 도구**이며, 함수/import/의존성 체인 등 실제 아키텍처 구조로부터 그래프를 구축합니다. 1차 목적은 코딩 에이전트의 코드베이스 온보딩 비용/토큰 절감입니다.

**Graphify vs 법령/기능성 RAG — 완전히 별개**

| 구분 | Graphify | RAG (법령/기능성 검색) |
|---|---|---|
| 입력 | 프로젝트 소스코드 | 도메인 문서 (법조문, 기능성 정보) |
| 목적 | 코딩 에이전트가 코드 구조를 빠르게 파악 | 사용자 질의에 관련 정보를 찾아 답변 생성 |
| 그래프의 노드 | 함수, 클래스, 파일, import 관계 | 법조문, 시행령, 참조 관계 |
| 실행 시점 | 개발 중 코딩 에이전트가 참조 | 서비스 런타임에 사용자 질의 처리 |

→ **Graphify는 코드베이스(`apps/`, `services/`) 온보딩 전용으로 한정**하고, 법령/기능성 도메인 지식 그래프에는 사용하지 않습니다(1.4절의 pgvector 스키마로 별도 처리).

---

## 3. Obsidian Vault 운영 방식

### 3.1 Vault를 프로젝트별로 둘지, 전역으로 둘지

**프로젝트 내부에 개별 Vault를 두는 방식의 문제점**: Obsidian은 Vault 간 위키링크(`[[..]]`)나 그래프 뷰가 Vault 경계를 넘지 못합니다. 프로젝트를 이동/삭제할 때 `.obsidian` 설정이 프로젝트 저장소 git 히스토리에 섞여 지저분해질 수 있고, 여러 Vault를 열어두면 컨텍스트 스위칭 비용이 발생합니다.

**대안**
1. **단일 전역 Vault + 프로젝트별 폴더 + symlink**: `Vault/Projects/프로젝트명/` 구조로 두고, 심볼릭 링크로 실제 프로젝트 디렉토리의 특정 폴더를 Vault 안으로 끌어옴. Vault는 하나라 링크/그래프가 다 연결되고, 실제 파일은 프로젝트 저장소에 존재해 git으로 관리됨.
2. **완전 분리**: 프로젝트 쪽은 `docs/` 폴더 + Markdown으로 두고, 전역 Vault에는 해당 프로젝트의 "메타 노트"(요약, 링크, 배운 점)만 관리.

**코딩 에이전트 관점에서 중요한 사실**: 코딩 에이전트(Claude Code 등)는 Obsidian의 Vault 개념 자체를 전혀 모릅니다. Vault는 Obsidian 앱이 인식하는 논리적 개념일 뿐, 파일시스템 레벨에서는 그냥 폴더입니다. 에이전트가 "프로젝트 Vault를 먼저 스캔하고 전역 Vault를 추가로 스캔"하는 자동 동작은 없으므로, 원하는 동작을 얻으려면 아래 중 하나로 명시적으로 설계해야 합니다.
1. `CLAUDE.md` / 프로젝트 설정 파일에 전역 Vault 경로를 명시.
2. MCP 서버로 Obsidian Vault를 연결하고, "먼저 프로젝트 Vault 검색 → 없으면 전역 Vault 검색" 순서를 지시사항에 명시.
3. symlink로 물리적으로 한 곳에 모으기(대안 1과 동일).

### 3.2 회의록/이슈 아카이빙 — 3계층 구조

**Obsidian의 물리적 처리 능력은 걱정할 필요가 거의 없습니다.** 파일 기반이라 노트가 수만 개까지 가도 앱이 못 열 정도로 느려지진 않습니다. 진짜 문제는 **신호 대 잡음비**입니다 — raw export를 계속 쌓으면 검색 시 관련 없는 옛날 자료가 잔뜩 걸리고, "이 결정이 왜 이렇게 됐지?" 질문에 답하려면 수백 개 노트를 뒤져야 하는 문제가 생깁니다.

**추천 구조: 3계층**
- **Layer 1 — Raw (원본, 거의 손 안 댐)**: 회의록/이슈 export를 그대로 날짜별/ID별 폴더에 기계적으로 적재. "원장(ledger)" 역할.
- **Layer 2 — Distilled (사람이 요약)**: 매주/매 스프린트마다 raw 노트를 보고 "이번 주 결정된 것 3~5줄"로 요약. Layer 1의 개별 노트를 링크로 참조. 실제로 검색/그래프에서 주로 마주치는 레이어.
- **Layer 3 — Decisions / Knowledge (영구 지식)**: "이 아키텍처를 왜 이렇게 정했나" 같은, 시간이 지나도 유효한 결론만 모은 노트. **ADR(Architecture Decision Record) 형식**이 잘 맞음.

**아카이빙 권장 사항**: 완전 삭제나 콜드 아카이빙은 비추천(근거 추적 단절). 대신 날짜 기반 폴더 구조 + 오래된 폴더는 Obsidian의 "Excluded files" 설정으로 검색 인덱스에서만 제외하는 방식을 추천.

**QMD(Query Markup Documents) — Vault 검색 효율화 검토 후보**: Raw 노트가 실제로 쌓여 검색 관련성 저하가 체감되는 시점에, [`tobi/qmd`](https://github.com/tobi/qmd)(BM25+벡터+LLM 재랭킹 하이브리드 검색, MCP 서버 내장)를 도입하는 것을 검토할 수 있습니다. 아직 회의록/이슈가 문제가 될 만큼 쌓이지 않은 초기 단계이므로 지금은 확정 스펙이 아니라 향후 검토 후보로 기록해 둡니다. 도입 시 QMD는 어디까지나 Vault(회의록·노트) 검색용이며, 1.4절의 법령/기능성 RAG 파이프라인(pgvector 기반)과는 별개 트랙임을 구분해야 합니다.

---

## 4. 포트폴리오 데모 환경

이 프로젝트는 포트폴리오로 활용될 예정이며, 채용 담당자가 별도 설치 부담 없이 직접 체험할 수 있는 데모 환경이 필요합니다.

### 4.1 배경 — 왜 로컬 Docker 데모인가

LangGraph, Tool Calling/Routing, RAG 기능의 실제 동작을 확인하려면 LLM과 임베딩 모델이 필요합니다. 사이드 프로젝트 규모에서 별도 클라우드 호스팅으로 퍼블릭 데모를 상시 제공하면 LLM 토큰 비용, 메모리, 네트워크 비용이 크게 소모될 것으로 예상되므로, **채용 담당자의 로컬 환경에서 Docker로 구동하는 방식**을 기본으로 합니다.

### 4.2 구성 원칙

- **Docker Compose**를 적극 활용합니다. FE/BE/데이터베이스가 각각의 컨테이너로 구성될 것을 전제로 합니다.
- 채용 담당자는 충분한 개발 지식이 부족할 수 있음을 가정하고, **최소한의 CLI 명령어**로 데모를 체험할 수 있어야 합니다.
- **사전 조건**: RAG 구동 확인을 위한 임베딩 모델이 필요하므로, 로컬 `ollama`와 `qwen3-embedding:0.6b` 모델의 설치 및 연동 방법을 README.md에 포함합니다. LLM API 키 입력 준비가 필요하다는 안내도 사전 조건에 포함합니다.

### 4.3 컨테이너 구동 시점의 흐름

- 담당자가 자신이 보유한 **LLM API 키를 입력**하도록 입력 인터페이스를 제공하고, 절대 유출되지 않도록 담당자 로컬 환경의 격리된 영역에 저장한 뒤 프로젝트가 가져다 쓰도록 구성합니다.
  - 입력 인터페이스를 브라우저로 할지, CLI로 할지, 다른 수단으로 할지는 **인터뷰 과정에서 확정**합니다.
- 담당자에게 입력을 받는 시점에:
  - 입력 요청 및 응답 등 CLI 메시지는 모두 **한글**로 구성합니다.
  - 유출되지 않는 안전한 로컬 환경에 저장된다는 안내 문구를 포함합니다.
- 기본적인 **데이터베이스 시딩**이 필요하며, 이 시점까지 RAG를 위해 pgvector에 임베딩되어 있는 데이터까지 시딩하도록 구성합니다.
  - 시딩 데이터에는 **가상의 고객들이 자신이 섭취 중인 영양제(건강기능식품) 성분명을 미리 입력해 둔** 가정 데이터를 포함합니다(고객 : 성분 = 1 : N). 예: 비타민A/150mg, 비타민B1/500mg, 비타민B2/1000mg, 마그네슘/1000mg, 피크노제놀/150mg 등.

### 4.4 기본 로그인 기능

- **소셜 로그인은 제외**합니다 — 채용 담당자가 자신이나 회사 정보가 외부로 전송된다는 느낌을 줄 수 있기 때문입니다.
- 기본 로그인 계정 정보도 4.3절의 시딩 데이터에 포함합니다.

### 4.5 웹 챗봇

- 웹 서비스 환경의 주 목적은 건강기능식품의 기능성이나 관련 법령을 참고하여 **광고 전략이나 제한사항, 가상 고객과의 상담 과정**에서 필요한 정보를 제공하는 것입니다.
- 정보 제공/RAG 기능을 MCP로 wrapping해 노출하는 것이 기본 방향이지만, 채용 담당자가 자신이 쓰는 LLM 앱/에이전트에 새로운 MCP를 연동하는 것을 원하지 않을 수 있습니다. 이를 보완하기 위해 **최소한의 기능을 갖춘 챗봇을 웹 환경에 직접 탑재**합니다.
- 챗봇 자체 기능과 Tool Calling, RAG 검색 등 내부 기능 활용에는 LLM과 임베딩 모델이 필요하므로, 담당자가 LLM API 키를 입력하지 않았거나 임베딩 모델 설정을 완료하지 않은 경우 **다시 입력하도록 유도**하는 로직을 포함합니다.

---

## 5. 스펙 정의 에이전트를 위한 요약 체크리스트

인터뷰 시 아래 항목들이 이미 사전 조사·검토를 거쳐 확정된 배경지식임을 참고해 주세요. 세부 확정은 인터뷰를 통해 진행합니다.

- [ ] **⭐ 대상 법령**: 건강기능식품에 관한 법률, 식품 등의 표시ㆍ광고에 관한 법률(각 시행령·시행규칙 포함), 건강기능식품의 표시기준(행정규칙/고시). 사용 시나리오는 "건강기능식품 관련 기업 실무 담당자". 식품위생법·전자상거래법 등은 참조 시에만 개별 수집하는 2차 대상(1.1~1.2절)
- [ ] **프로젝트 도메인**: 건강기능식품 관련 법령/기능성 정보 서비스 (정적 정보 웹페이지 + 변경 가능 정보 RAG/MCP 이원화)
- [ ] **RAG 노출 방식**: FastMCP로 MCP 서버화 → Claude Desktop 등 MCP 클라이언트에서 법령/기능성 정보 조회, 동시에 웹 챗봇에서도 내부적으로 활용(4.5절)
- [ ] **학습 목적 기술**: Obsidian(Vault 설계), RAG, LangGraph, Tool Calling/Routing, FastMCP, pgvector
- [ ] **웹서비스 스택 후보**: Nest.js(일반 웹서비스, FE 미정) + Python 서비스(Django or FastAPI, RAG+LangGraph 학습용)
- [ ] **데이터베이스**: PostgreSQL + pgvector (임베딩 + 관계형 참조를 한 스택에서 처리, Neo4j는 현재 불필요 — 1.4절)
- [ ] **법령 참조 관계 저장 구조**: `legal_provisions` + `provision_references` 하이브리드 스키마(1.4절), 대상 법령은 1.1절 기준. 위임 조항 처리 시 **대통령령뿐 아니라 총리령**도 함께 확인 필요(1.2절)
- [ ] **데이터 소스 — 확정/시행예정 법령**: 국가법령정보센터 API — **Key 발급 완료**. 시행 예정 조문까지 이 API 하나로 커버(1.3절). 「건강기능식품의 표시기준」 같은 행정규칙/고시도 동일 API로 조회 가능한지는 인터뷰에서 재확인 필요(1.1절)
- [ ] **데이터 소스 — 건강기능식품 기능성 정보**: 식약처 식품안전나라 공공데이터 플랫폼 — **API Key 발급 및 사용 승인 완료**. 대상 데이터셋 4종: ① 개별인정형 정보(I-0050), ② 기능성 원료인정현황(I-0040), ③ 품목제조 신고사항 현황(I0030), ④ 품목제조신고 원재료(C003). **API 스펙/요청·응답 스키마는 별도 전달 예정** — 인터뷰 시점에 없다면 먼저 확보 여부 확인. **호출 횟수 제한(시간당 100회), 정렬 불가, 페이지네이션 제약**이 있어 CSV 사전 적재 + `total_count` 기반 증분 감지 전략을 제안해둠(1.5.1~1.5.2절) — 고유 ID 필드 통일 여부는 API 응답 스키마 확보 후 재검증 필요
- [ ] **Graphify 사용 범위**: 코드베이스(`apps/`, `services/`) 온보딩 전용. 법령/기능성 도메인 지식 그래프에는 미사용(2.4절)
- [ ] **Obsidian Vault 운영 방식**: 전역 Vault + symlink 또는 MCP 연결로 프로젝트 로컬/전역 지식 통합 검토 중. 3계층(Raw/Distilled/Decisions) 구조로 회의록·이슈 누적, QMD는 향후 검토 후보(3장)
- [ ] **포트폴리오 데모 환경**: Docker Compose 기반 원클릭 구동, ollama+qwen3-embedding:0.6b 로컬 임베딩, LLM API 키 로컬 격리 저장(입력 인터페이스는 인터뷰에서 확정), 한글 CLI 메시지, 고객:성분=1:N 시딩 데이터, 소셜 로그인 제외, 최소 기능 웹 챗봇(4장)
- [ ] **미확정 사항** (인터뷰에서 다뤄야 함): FE 프레임워크, 배포/인프라, 구체적 RAG 청킹 전략, 임베딩 모델 선택(qwen3-embedding:0.6b 외 대안 포함), LangGraph 그래프 세부 노드 설계, MCP 서버 인증 방식, 테스트/평가(evaluation) 전략, 식약처 4개 API 요청/응답 스키마 확정 및 고유 ID 필드 통일 여부, 식약처 데이터의 RAG 채택 여부, 개별인정형/고시형 중 다룰 범위, 품목제조 신고사항(I0030/C003) 데이터의 증분 감지 전략 최종 확정(1.5.2절 제안안 검토), LLM API 키 입력 인터페이스 방식(브라우저/CLI/기타), 기본 로그인 인증 방식 세부 설계, QMD 도입 여부 및 시점, 「건강기능식품의 표시기준」의 `law_type` 스키마 값 확정

---

## 부록: 참고 자료

- 국가법령정보센터: `https://open.law.go.kr` — API Key 발급 완료
- 식약처 식품안전나라 공공데이터 활용 플랫폼: `https://www.foodsafetykorea.go.kr/api/datasetList.do?menu_grp=MENU_GRP31&menu_no=17879` — API Key 발급 및 사용 승인 완료 (I-0050, I-0040, I0030, C003 4개 서비스)
- QMD (Vault 검색 효율화 검토 후보): `https://github.com/tobi/qmd`
- 참고 확인 사례: 「조달사업에 관한 법률 시행령」(대통령령 제36632호, 2026.9.1. 일부개정) — 국가법령정보센터에서 시행 예정 조문 표기 방식을 확인한 근거 자료(1.3절)


---

## ⚠️ API 실측 기록 (2026-09-08, 수집기 구현 중 확정)

### 국가법령정보센터 DRF API
- 검색: `https://www.law.go.kr/DRF/lawSearch.do?OC=<키>&target=law&type=JSON&search=1(법령명 검색)&query=<법령명>`
  - `search=2`는 본문 검색(전체 목록 반환)이므로 **법령명 검색은 `search=1` 사용**
  - 응답 `LawSearch.law`는 **1건=dict, N건=list**로 타입이 변함 → 정규화 필수
- 본문: `https://www.law.go.kr/DRF/lawService.do?OC=<키>&target=law&MST=<법령일련번호>&type=JSON&efYd=<시행일8자리>`
  - **`efYd`는 필수급**: 검색 응답 `법령상세링크`에 포함된 시행일을 그대로 재사용해야 정확한 시행일본문이 나옴
  - 누락 시 완전히 다른 법령이 반환되는 오류 실측 (MST=276693 + efYd=20251001 → 가축분뇨법 반환)
  - 조문 데이터: `법령.조문.조문단위[]` (조문번호/조문여부/조문제목/조문내용/조문시행일자), 부칙: `법령.부칙.부칙단위`
  - 내용 필드(`조문내용` 등)는 `str` 또는 `list[str]` → 정규화 필수
- **행정규칙(고시·예: 건강기능식품의 표시기준)은 `target=admrul`로 조회 가능** (사용자 피드백 기반 실측 확정 2026-09-08):
  1. 목록: `https://www.law.go.kr/DRF/lawSearch.do?OC=<키>&target=admrul&type=JSON&query=<규칙명>`
     응답 `AdmRulSearch.admrul`의 `행정규칙일련번호` 확인 (정확매칭: `행정규칙명`==검색어 && `현행연혁구분`=="현행")
  2. 본문: `https://www.law.go.kr/DRF/lawService.do?OC=<키>&target=admrul&type=JSON&ID=<행정규칙일련번호>`
     응답 `AdmRulService.조문내용` = list[str] (각 원소가 "제N조(...)" 형식, 조문형식여부=Y 기준)
     참고: `target=admRuleLsLst` 같은 변형은 빈 응답 → `admrul`이 정답
- 실측 적재: 건강기능식품의 표시기준(고시) 10조문, law_type=administrative_rule
- 위임 표현의 실제 문형: "~으로 **정하는 바에 따라**"가 다수("으로 정한다"는 소수). 참조 엣지 추출 시 양쪽 패턴 모두 커버.

### 식품안전나라 openapi 호출 제한 (운영 필수)
- **KST 09:00~19:00 호출 차단 (ERROR-503)** → 수집/증분 잡은 19시 이후 실행
- 증분 파라미터는 최대 7일(ERROR-303), 1회 최대 1,000건(ERROR-336), 증분 미사용 시 시간당 100회(INFO-320)
- 상세 코드표: `foodsafetykorea_schema/api_response_schema_doc.md`

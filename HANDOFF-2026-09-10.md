# 📋 Handoff — nutrition-mind Epic #2 완료 후 인수인계

> 작성: 2026-09-10 (Epic #9 — 담당자용 확장 완료 시점)
> 후속 에이전트가 새 Epic을 시작하기 위한 문서. 중복 설명 대신 경로·URL로 참조.
> `.env`의 API 키 값은 절대 출력·복사 금지. 커밋·푸시 정책은 하단 참조.

---

## 1. 프로젝트 한 줄 요약

건강기능식품 법령·기능성 정보 RAG·MCP 데모에서 시작해, **건기식 기업 담당자(영업·상담 / 제품기획·마케팅 / 총관리자)가 활용하는 도구로 확장**을 완료한 상태. Epic #1(데모 구축)과 Epic #2(담당자용 확장) 모두 종료. 후속 Epic(#3~)은 Tool Calling/Routing 학습 + Obsidian Vault 실제 도입 + 남은 백로그가 후보.

---

## 2. 현재 상태 (한 줄 요약 + 핵심 수치)

- **법령 RAG**: 조문+항·호 병합 본문 148,470자(구 63,453자 → +134%), 임베딩 426/426, **Recall@5 1.000 / MRR 0.833**(13문항 골드셋)
- **식약처 데이터**: 5개 API 93,273건 적재, I0030(품목제조신고) 45,996건 정규화 백필 완료
- **참조 엣지**: 233건(cites 186 + delegates_to 47), 가지번호("제N조의2") 매칭 수정 완료
- **통합 테스트**: 13개 스크립트 **203케이스 전부 통과**(회귀 스크립트 목록: backend/package.json scripts)
- **Docker Compose 스택 구동 중**: frontend 3000 · backend 3001 · mcp 8000 · db 5433(호스트)
- **Prisma migrate 전환 완료**: `prisma/migrations/` 7건 체인(extensions→init→trgm→add_name→drop_name_enc→foodsafety_manufacture→recommendation_manufacture), `migrate status: up to date`

핵심 참조 문서(중복 설명 대신 읽을 것):
- `README.md` — 실행법, 역할별 데모 계정, 역할별 화면 구성, 데모 확인 절차, Epic 1·2 구현 현황
- `AGENTS.md` — 에이전트 운영 규칙(한국어 문서, 커밋 정책, .env 키 비출력 등)
- `CONTEXT.md` — 도메인 용어집(담당자·고객·성분/원료 구분·성분 갭·접근 로그·문구 검증 등 15개 용어)
- `docs/adr/0001` · `docs/adr/0002` — 스코프 확장·운영 가정, 개인정보 취급 원칙(+2026-09-10 개정: 이름 평문 전환)
- `DESIGN.md` — 마스터카드 인스파이어드 디자인 시스템(토큰은 `frontend/lib/design.ts`로 번역)
- `seed.yaml` — Epic #1의 1차 스펙. 일부 제약은 Epic #2에서 갱신됨(ADR-0001 참조)
- GitHub Issues: #1~#29 전부 CLOSED — 티켓별 검증 증거는 이슈 코멘트에 있음

---

## 3. 코드베이스 구조 요약 (Epic #2 이후)

```
backend/src/
  auth.controller.ts       — JWT 로그인(bcrypt) + 역할 클레임
  customers.controller.ts  — 고객 CRUD(복호화)·섭취 제품·성분 갭·추천 생성/상태 관리
  chat.controller.ts       — 상담 챗봇 + 고객 컨텍스트 개인화(customerId 옵션)
  marketing.controller.ts  — 표시·광고 문구 검증(RAG 근거 판정)
  admin.controller.ts      — 총관리자 접근 로그 열람
  ingredients.controller.ts — 성분 마스터 목록
  products.controller.ts   — 품목제조신고·원료 검색
  crypto.service.ts        — envelope encryption(KEK→DEK 2계층, 연락처·메모 암호화)
  ingredient-mapping.service.ts — 원료 텍스트→성분 키워드 규칙 매핑(순수 함수)
  recommendation.service.ts — 갭→제품 후보 매칭·랭킹(순수 함수)
  access-log.service.ts    — 개인정보 접근 기록
  rag-search.service.ts    — 법령 pgvector 검색(chat·marketing 공용)
  roles.decorator.ts / roles.guard.ts — 간소화 RBAC
frontend/app/
  login/ · page.tsx(워크스페이스 홈 허브) · chat/ · customers/ · customers/[id]/ · marketing/ · admin/
frontend/lib/auth.ts      — 세션 헬퍼 · frontend/lib/design.ts — DESIGN.md 토큰
frontend/app/_components/spinner.tsx — 로딩 스피너
ingest/                   — 법령 수집·임베딩·식약처 CSV 적재·RAG 평가·참조 엣지 추출
prisma/migrations/        — 7건 체계(extensions→init→trgm→add_name→drop_name_enc→foodsafety_manufacture→recommendation_manufacture)
mcp-server/server.py      — FastMCP 도구 4종
```

---

## 4. 완료된 작업 요약 (Epic #2)

| 이슈 | 결과 | 검증 수치 |
|---|---|---|
| #10 법령 본문 복원(P0) | 조문+항·호 병합 재수집·재임베딩 | 본문 +134%·소실 66개 복원·Recall@5 1.000/MRR 0.833 |
| #11 역할·간소화 RBAC | 3역할 가드·bcrypt·로그인 분리 | 17/17 |
| #12 제품 검색 기반 | 정규화 4칼럼·91,992건 백필·검색 API | 20/20 |
| #13 고객·개인정보 | envelope encryption·마스킹 | 20/20 + 평문 부재 SQL |
| #14 접근 로그 | 기록·총관리자 열람 | 8/8 |
| #15 섭취 제품 등록 | 연결·수동·스냅샷 | 15/15 |
| #16 성분 마스터·갭 | 17종·키워드 매핑(tdd)·근거 | 매핑 24/24·갭 27/27 |
| #17 추천 제안 | 갭→매칭·근거 랭킹·수용/보류 | 단위 10/10·통합 13/13 |
| #18 챗봇 개인화 | 별도 페이지·컨텍스트 주입 | 11/11 |
| #19 문구 검증 | RAG 판정(4종) | 11/11 |
| #20 리서치 | 원료형 정규화·섭취량/주의사항 | 9/9 |
| #21 폴리시·README | DESIGN.md 토큰·데모 절차 | 라우트 8건 200 |
| #22 참조 엣지(후속) | 가지번호 매칭 수정·233건 | 실측 |
| #23~#25 버그 | 목록 노출·기능성 바인딩·스피너/버튼 토큰 | 223케이스 전부 통과 |
| #26 추천 검색 I0030 한정 | C003 제외 | 회귀 통과 |
| #27 Prisma migrate 전환 | db/init·db/migrations 제거·샘플 DB 재현 검증 | migrate status: up to date |
| #28 이름 평문 전환 | 마스킹·암호화 제외·ADR-0002 개정 | 20/20 |
| #29 업소명·생산종료여부 | 45,996건 백필·추천 스냅샷·UI 배지 | E2E 실측 |

---

## 5. 남은 백로그 (우선순위 순)

1. **별표 PDF 파싱** — 표시기준 별표 문서의 PDF 포맷이 다양해 카테고리화 불가 판단으로 별도 Epic으로 미룸. 사용자가 별표·서식 확인 후 범위 확정 필요(배경: 이슈 #3 종료 코멘트)
2. **증분 갱신 스케줄러** — 식품안전나라 19시 이후 cron 증분 갱신. 현재는 수동: `../ingest/load_foodsafety_csv.py` + 정규화 백필 스크립트(`ingest/backfill_product_columns.py` — 멱등, 재실행 안전)
3. **LLM 합성 고도화** — 현재 근거 나열 + ollama 합성. 고객 컨텍스트 주입은 #18에서 완료했으나, 챗봇의 근거 조문 연결 품질 개선 여지
4. **LLM 보조 추천** — #17의 규칙 기반 추천을 보조하는 LLM 자유 제안(후순위로 백로그에 기록됨)
5. **원료명 검색 튜닝** — raw_material_name ILIKE 검색 ~0.7s(장문 칼럼) — 쿼리·인덱스 튜닝 여지
6. **증분 적재행 정규화 백필 자동화** — 신규 적재 foodsafety_rows는 정규화 칼럼이 미채움이므로 `ingest/backfill_product_columns.py` 멱등 재실행 필요
7. **관리자 계정·역할 운영 UI** — /admin의 시스템 관리 카드(후속 확장)
8. **QMD 도입** — Raw 노트 500개 이상 시 검토(현 단계 미도입)
9. **브라우저 시각 확인** — DESIGN.md 폴리시(이슈 #21)의 코드 수준 검증은 완료했으나 브라우저 스크린샷 기반 시각 비평은 사용자 확인 권장(스크린샷 도구 부재)

---

## 6. 다음 Epic 제안 — Tool Calling / Tool Routing & Selection + Obsidian Vault 도입

**배경**: `project-background-context.md` 2장(212~270행)에서 사용자가 학습을 원했던 핵심 개념이나, 현재 코드베이스에는 **Tool Calling 루프와 Tool Routing/Selection이 구현돼 있지 않다.**

현재 상태:
- MCP 서버(mcp-server/server.py)에 도구 4종이 노출돼 있으나, **백엔드에서 이 도구들을 LLM 루프로 호출하는 로직이 없다.** 챗봇의 법령 RAG는 chat.controller.ts에서 직접 pgvector를 질의하는 단일 경로이고, LangGraph 6단계 노드 구조는 README에 언급돼 있으나 실제 코드에는 도구 라우팅·선택 로직이 없다.
- 즉, `project-background-context.md` 2.1절의 "에이전트 하네스 루프"(① 스키마 전달 → ② tool_use 반환 → ③ 백엔드 실행 → ④ 재호출 → ⑤ 서빙)가 구현되지 않은 상태다.

다음 Epic에서 학습할 수 있는 내용(배경 문서 2장 참조):

1. **Tool Calling 루프 구현** — chat.controller.ts에 도구 스키마 주입: MCP 도구 4종(search_legal_provisions·get_functional_ingredient·get_product_report·get_notified_functionality) + 이번 Epic의 신규 도구(고객별 성분 갭 조회·섭취 제품 조회·추천 제안)를 도구 스키마로 LLM에 전달 → tool_use 파싱 → 실행 → 재호출 루프
2. **Tool Routing/Selection** — 도구가 20개 이상으로 늘어나면 배경 문서 2.2절의 방식 적용:
   - 카테고리/도메인 기반 사전 필터링(초심자 추천 — 배경 문서 권장)
   - 임베딩 기반 유사도 검색(도구 설명을 임베딩해 질문과 top-k 선택 — RAG 원리 재활용)
   - 계층적 라우팅(서브 에이전트 분할 — 도구 수백 개 대규모 환경)
3. **LangGraph 도입 판단** — 배경 문서 2.3절: 도구 1~3개는 순수 코드, 10개 이상은 프레임워크. 현재 4종이지만 담당자용 도구(고객 갭·추천·문구 검증 등)를 추가하면 8개 이상으로 성장 — LangGraph 전환 판단 지점에 도달
4. **Graphify와의 구분** — Graphify는 코드베이스 온보딩 전용으로만 사용(배경 문서 2.4절, seed.yaml 비-목표 유지)
5. **Obsidian Vault 실제 도입 + QMD 검색** — 사용자가 학습을 원했으나 현재 **Vault가 실제로 활용되고 있지 않다**(아래 상세).

### Obsidian Vault 도입 (배경 문서 3장 + `docs/obsidian-vault.md` — 설계 완료, 실행 미수행)

`docs/obsidian-vault.md`와 `project-background-context.md` 3장(282~310행)에 설계가 확정돼 있으나, **전역 경로(`~/ObsidianVault/`)에도 프로젝트 경로에도 `.obsidian` 디렉터리가 없고 실제로 활용되고 있지 않다.**

설계된 구조(문서에 확정 — 실행만 남음):

- **단일 전역 Vault + symlink**: `~/ObsidianVault/Projects/nutrition-mind/` 구조. Vault가 하나라 위키링크·그래프가 연결되고, 실제 파일은 프로젝트 저장소에 존재해 git 관리 유지
- **3계층 아카이빙**: Layer 1 Raw(회의록·이슈 export 원본, 기계적 적재) / Layer 2 Distilled(주간·스프린트 결정 3~5줄 요약 — 사람이 작성) / Layer 3 Decisions(ADR — 영구 보존, 검색 대상). 삭제·콜드아카이브 금지 — 오래된 Raw는 Obsidian "Excluded files" 설정으로 검색 인덱스에서만 제외
- **코딩 에이전트 연동 규칙**(배경 문서 3.1절): 에이전트는 Vault 개념을 모르므로 명시적 설계 필요 — symlink로 물리적으로 한 곳에 모으거나, CLAUDE.md·AGENTS.md에 전역 Vault 경로 명시, 또는 MCP 서버로 Obsidian Vault 연결 후 "프로젝트 Vault 검색 → 없으면 전역 Vault 검색" 순서 지시
- **QMD 최소 연동 설계**(docs/obsidian-vault.md 4절): [`tobi/qmd`](https://github.com/tobi/qmd)(BM25+벡터+LLM 재랭킹 하이브리드 검색, MCP 서버 내장) — Raw 노트 500개 이상 시 도입 검토(현 단계 미도입). 도입 시 QMD는 Vault 검색 전용이며 법령/기능성 RAG(pgvector)와 별개 트랙

다음 Epic에서 실행할 작업:

1. 전역 Vault 생성: `mkdir -p ~/ObsidianVault/{Projects/nutrition-mind,Decisions,Raw/2026-09}` (docs/obsidian-vault.md §5 체크리스트)
2. symlink 연결: `ln -s /Users/yulaeun/nutrition-mind/docs ~/ObsidianVault/Projects/nutrition-mind/docs`
3. 확정 결정 ADR화: Prisma v7 고정·admrul 경로·5433 포트 등 → `~/ObsidianVault/Decisions/`에 기록(docs/obsidian-vault.md §5 체크리스트 참조)
4. Obsidian 앱에서 Vault 열기 → `.obsidian` 설정이 전역 Vault에만 생성됨을 확인
5. ADR·회의록·이슈 검증 증거를 Decisions·Raw 계층에 적재하고, Obsidian 그래프 뷰·위키링크가 정상 동작하는지 확인
6. (도입 시점 도래 시) QMD MCP 서버 설치·설정 — Raw 노트 500개 이상이 트리거(현 단계에서는 설계만 유지)
7. 코딩 에이전트 연동 규칙을 AGENTS.md에 명시(전역 Vault 경로·검색 순서)

추천 방향: Tool Calling/Routing(#3~#4)과 Obsidian 도입(#5~#7)을 Epic 3으로 함께 구성하면 "에이전트 하네스 학습 + 지식 관리 체계 실제 가동"이라는 학습 목표와 실용 목표가 결합된다. MCP 도구를 별개로 분리 등록하고 질문 의도에 따라 라우팅하는 방식이 배경 문서의 권장 방식이다. QMD도 MCP 서버이므로 Tool Calling 학습과 자연스럽게 결합된다.

---

## 7. 환경 제약 (이전 핸드오프 계승 — `HANDOFF-2026-09-09.md` 참조)

- **Docker**: `export PATH="/opt/homebrew/bin:/Applications/Docker.app/Contents/Resources/bin:$PATH"; export DOCKER_CONFIG=/tmp/dockercfg` 필요. `docker compose` 서브커맨드가 아닌 `docker-compose`(하이픈)를 사용할 것 — `docker compose`는 미지원. 스택 구동 중: frontend 3000 · backend 3001 · mcp 8000 · db 5433(호스트)
- **DB**: `DATABASE_URL`은 루트 .env에서 (호스트 5433·포트 충돌: Homebrew Postgres가 5432 점유). DB 롤 nutrition은 superuser(CREATEDB·ShadowDB 자동 생성 가능)
- **npm**: `export npm_config_cache=/tmp/npm-cache` 필수
- **Python venv**: `/tmp/nm-venv`(psycopg·dotenv·requests·cryptography 설치). 재부팅 후 소실 시 재생성
- **Ollama**: 127.0.0.1:11434(호스트)·host.docker.internal(컨테이너). qwen3-embedding:0.6b(dim=1024)·glm-5.3-flash:cloud
- **식품안전나라 openapi**: KST 09:00~19:00 호출 차단(ERROR-503) — 호출 불필요한 작업은 이 시간대에도 피하지 않아도 되지만, 호출이 필요하면 19시 이후
- **Prisma migrate**: `npx prisma migrate deploy`(적용) · `npx prisma migrate dev --name X`(ShadowDB 자동) · `npx prisma migrate status`(드리프트 확인). Datasource URL은 `prisma.config.ts`에서 process.env.DATABASE_URL로 읽음
- **seed**: `npx prisma db seed` — 멱등. 역할 3종·데모 계정 4종·고객 20명·성분 마스터 17종
- **MCP 서버(mcp-server/server.py)**: FastMCP 도구 4종. 토큰 인증(Authorization: Bearer MCP_TOKEN)
- **고객 이름 유실 복구**: customer 5893166a-11ec-494d-8baf-2224f33febeb의 이름은 "(이름 복구 필요 — 수정해 주세요)" 플레이스홀더로 표시됨 — 사용자가 목록에서 수정 필요. 정하윤·오예린·신동현 3행은 프로필이 동일(성분 없음·연락처 암호화)하여 이름 배정이 임의(이슈 #23 코멘트에 명시)

---

## 8. 운영 규칙 (이슈·커밋 정책 — 사용자 확정)

- **커밋**: 에이전트가 제안 커밋 메시지(한국어, "feat:|fix:|docs:|polish: … (이슈 #N)" 형식)로 직접 수행. **push는 금지**. 1인 프로젝트이므로 브랜치 충돌 여지 없음
- **커밋 author**: repo `git config user.name`(raeun)·`user.email`(raeoon0511@naver.com) 그대로 사용
- **권한 문제 발생 시**: 사용자에게 요청으로 폴백
- **이슈 카드**: 작업마다 Epic #9 하위에 카드 생성(ready-for-agent 라벨) → 구현·검증 → 이슈 코멘트로 증거 기록 → 종료
- **문서**: 한국어로 작성(AGENTS.md 운영 규칙). 용어는 `CONTEXT.md` 준수(성분/원료 혼용 금지)
- **서브에이전트**: 병렬 가능 티켓은 서브에이전트 백그라운드 위임. **장기 무응답(파일·DB·이슈 기록 없이 수십 분)이 관찰되면**: ① send_message로 기록 요청 ② 1~2라운드 내 응답 없으면 interrupt → 컨텍스트 유지 상태로 재개(send_message) ③ 재개 후에도 무진전이면 신규 에이전트로 교체하거나 오케스트레이터가 직접 구현(#13·#17 선례). 중단 후에는 큐잉된 메시지가 자동 전달되지 않으므로 **반드시 새 send_message로 재개**할 것
- **Ouroboros**: 사용하지 않음(HANDOFF-2026-09-09.md 참조)

---

## 9. 이슈 트래커 상태 (Epic #9 + 후속)

전부 CLOSED: #10~#21(Epic 티켓 12건) · #22(참조 엣지) · #23(추천 목록+근거 칼럼) · #24(리서치 기능성 바인딩) · #25(스피너·버튼 토큰) · #26(C003 제외) · #27(Prisma migrate 전환) · #28(이름 평문 전환) · #29(업소명·생산종료여부)

Epic #9(담당자용 확장): https://github.com/RaeunYu/nutrition-mind/issues/9
Epic 완료 요약 코멘트: https://github.com/RaeunYu/nutrition-mind/issues/9#issuecomment-5613454450

---

## 10. 이 세션에서 학습한 환경 특성 (기존 문서에 없음)

1. **서브에이전트 장기 무응답**: 에이전트가 30분+ 파일·DB·이슈 코멘트 모두 무기록인 패턴이 3회 반복됨. 중단(interrupt) 후 컨텍스트 유지 상태로 재개(send_message)하면 즉시 상태 기록·진행 재개 — **중단 후에는 큐잉된 메시지가 자동 전달되지 않으므로 반드시 새 send_message 필요**
2. **tsx 캐시**: `.ts` 파일 수정 후 tsx 실행 시 캐시 문제로 구버전이 로드될 수 있음 — `rm -rf node_modules/.cache ~/.cache/tsx` 후 재실행
3. **package.json JSON 파손**: 스크립트 삽입 시 쉼표 누락 발생 가능 — 편집 후 `python3 -c "import json; json.load(open('package.json'))"`으로 검증
4. **psycopg 트랜잭션**: `conn.close()`는 미커밋 변경을 롤백함 — UPDATE 후 반드시 `conn.commit()`
5. **migrate dev의 드리프트 리셋 위험**: `migrate dev`는 드리프트 감지 시 리셋을 제안할 수 있음 — 실행 볼륨 DB에는 `migrate deploy`만 사용하고, `migrate dev`는 신규 변경 시에만 신중히 사용(리셋 제안이 나오면 거부)
6. **인덱스 명명 차이**: 수동 SQL로 생성한 인덱스(예: idx_access_logs_*)와 Prisma 자동 명명이 달라 `migrate dev`가 드리프트로 보고할 수 있음 — cosmetic이며 deploy 기반 운영에서는 무해. 남은 백로그: 인덱스 명명 정렬
7. **NestJS POST 응답 코드**: POST는 201을 반환하므로 테스트의 상태 코드 어서션은 200/201 모두 허용
8. **tsx는 .ts 파일만 트랜스파일** — .mjs 테스트 파일에 TS 문법(type 어노테이션)을 쓰면 SyntaxError. .mjs는 순수 JS로 작성하거나 .ts로 저장 후 tsx로 실행

---

## 11. 금지사항

- `.env`의 API 키 값 출력·복사 금지
- push 금지(로컬 커밋만 — 사용자 정책)
- Ouroboros 워크플로우·MCP 도구 호출 금지(사용 철회 확정 — HANDOFF-2026-09-09.md 참조)
- 식품안전나라 openapi를 KST 09:00~19:00에 호출 금지(ERROR-503)
- 대용량 CSV(foodsafetykorea_schema/C003.csv 86MB·I0030.csv 103MB) 전체 읽기 금지 — head·nrows 소량만
- 원시 SQL 직접 실행으로 암호화 우회 금지(개인정보는 CryptoService 경유만 허용)
- gh issue close는 검증 증거 기록 후에만 수행

---

## 12. 빠른 시작 (후속 에이전트 체크리스트)

1. `AGENTS.md` → `CONTEXT.md` → 필요한 문서만 선별 읽기(README·ADR·이슈)
2. `docker-compose ps`로 스택 구동 확인(미구동 시 docker-compose up -d --build)
3. `npx prisma migrate status`로 스키마 동기화 확인
4. `npm run test:customers` 등 관련 테스트로 기준선 확인
5. GitHub 이슈 상태·Epic #9 코멘트에서 최신 진행 상황 확인
6. 새 티켓 착수: 이슈 카드 생성(ready-for-agent) → 구현 → 통합 테스트 → 검증 증거 기록 → close → 커밋(제안 메시지 포함)